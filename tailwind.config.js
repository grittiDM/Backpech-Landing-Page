module.exports = {
  content: ["./*.html"],  // Monitora todos os HTMLs
  darkMode: "class",      // Integrado com seu JS existente
  theme: {
    extend: {
      colors: {
        primary: "#282828", // Suas cores personalizadas
      }
    }
  }
}