{
  "name": "backpech-assistencia",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx tailwindcss -i ./src/input.css -o ./assets/css/main.css --watch",
    "build": "npx tailwindcss -i ./src/input.css -o ./assets/css/main.min.css --minify",
    "clean": "rm -rf ./assets/css/main.css ./assets/css/main.min.css",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d .",
    "optimize-images": "npx sharp-cli input/*.{jpg,png} --output ./images/ --format webp --quality 80"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16",
    "autoprefixer": "^10.4.21",
    "gh-pages": "^6.1.0",
    "postcss": "^8.5.3"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.5",
    "sharp-cli": "^5.1.0",
    "tailwindcss": "^4.1.5"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
